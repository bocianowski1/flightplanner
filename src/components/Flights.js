import { Box, List, Stack, Heading, Input } from "@chakra-ui/react";
import FlightCard from "./FlightCard";

const Flights = () => {
  return (
    <List>
      <FlightCard />
    </List>
  );
};

export default Flights;
